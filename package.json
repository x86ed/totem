{
  "name": "totem",
  "version": "0.6.1",
  "description": "AI-native, git-native, distributed project management platform - open source and self-hosted",
  "main": "dist/init.js",
  "bin": {
    "totem": "./dist/init.js"
  },
  "scripts": {
    "start": "npm run build:scripts && node dist/server.js",
    "build:scripts": "tsc --project tsconfig.scripts.json",
    "dev:server": "ts-node scripts/server.ts",
    "test": "vitest",
    "test:scripts": "vitest run scripts/__tests__",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "init:contributor": "npm run build:scripts && node dist/init.js --type=config",
    "build": "npm run build:scripts && cd frontend && npm run build",
    "dev": "cd frontend && npm run dev",
    "clean": "rm -rf dist"
  },
  "keywords": [
    "ai-native",
    "git-native",
    "distributed",
    "project-management",
    "self-hosted",
    "open-source",
    "markdown",
    "kanban",
    "tickets",
    "roadmap",
    "totem"
  ],
  "author": "hi",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5"
  },
  "devDependencies": {
    "typescript": "^5.3.3",
    "ts-node": "^10.9.2",
    "@types/node": "^20.10.6",
    "@types/express": "^4.17.21",
    "@types/cors": "^2.8.17",
    "@types/supertest": "^2.0.16",
    "vitest": "^1.0.4",
    "supertest": "^6.3.3",
    "@vitest/coverage-v8": "^1.0.4",
    "textlint": "^15.0.1"
  }
}
